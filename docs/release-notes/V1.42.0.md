<div align="center">
  <img src="https://cdn.tw93.fun/pic/cole.png" alt="Diggory Logo" width="120" height="120" style="border-radius:50%" />
  <h1 style="margin: 12px 0 6px;">Diggory</h1>
  <p><em>Deep clean and optimize your Mac.</em></p>
</div>

### Changelog

1. **digg clean**: Project artifacts scan now finishes on huge workspaces and shows partial results when slow scans time out
2. **digg clean**: Typing a sudo password no longer gets treated as skipping sudo
3. **digg clean**: App leftovers can remove dead macOS app records from deleted apps and unmounted DMGs without deleting files
4. **digg clean**: Developer cleanup finds more rebuildable caches, including VS Code WebStorage, QQ, LM Studio, Folo, SenPlayer, Xcode simulator caches, and XCTest test data
5. **digg analyze**: Cached folders keep their results visible while Diggory refreshes in the background
6. **digg update**: Nightly updates skip reinstalling the same build, and `digg status` now matches the Mac App health signals more closely
7. **digg uninstall**: Related-file cleanup is stricter around system items, app helpers, symlinks, and container caches

### 更新日志

1. **digg clean**：Project artifacts 在超大工作区里不会再卡住，慢扫描超时后仍会展示已找到的结果
2. **digg clean**：在 sudo 提示里输入密码不会再被误判成跳过 sudo
3. **digg clean**：App leftovers 可以清理已删除应用、已卸载 DMG 留下的失效应用记录，且不会删除文件
4. **digg clean**：Developer cleanup 能发现更多可再生成缓存，包括 VS Code WebStorage、QQ、LM Studio、Folo、SenPlayer、Xcode 模拟器缓存和 XCTest 测试数据
5. **digg analyze**：打开已有缓存的文件夹时，后台刷新期间会继续显示已有结果
6. **digg update**：Nightly 更新不再重复安装同一个版本，`digg status` 也更接近 Mac App 的健康状态判断
7. **digg uninstall**：相关文件清理在系统项、应用 helper、symlink 和 container cache 上更谨慎

### Diggory Mac App

Prefer a GUI? Try [Diggory Mac App](https://diggory.fit), the CLI stays free and open source

### Thanks 💖

Issue reporters and PR contributors this cycle: @QasimXAli · @thedavidweng · @iammike2 · @wottpal · @seepine · @Saafo · @wupeter0001 · @sebastianbreguel · @Puhavik

> https://github.com/superstack-oss/Diggory-CLI
